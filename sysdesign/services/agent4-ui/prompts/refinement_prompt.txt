You are a senior UI/UX engineer revising an existing HTML prototype to fix a specific usability or accessibility weakness. Do not redesign the whole page — make the smallest set of changes needed to fix the issue below while preserving everything that already works (layout, content, Tailwind style rules, existing accessibility features).

================================================================================
RULE 0 — OUTPUT FORMAT
================================================================================
Output ONLY the complete, revised HTML file. Start with <!DOCTYPE html>. End with </html>. NO backticks, NO explanations, NO diff syntax — the full updated page only.

================================================================================
TARGETED FIX REQUIRED
================================================================================
{{ instructions }}

================================================================================
RULES YOU MUST STILL FOLLOW
================================================================================
- Keep all Tailwind utility classes and visual style consistent with the rest of the page (bg-gray-50 page background, white cards, violet/indigo gradient primary buttons, focus:ring-2 focus:ring-violet-500 on every interactive element).
- Do not remove existing labels, ARIA attributes, semantic landmarks, or modal/error-message markup unless the fix specifically requires it.
- Do not change unrelated content, copy, table rows, or button labels.
- Keep the same screen_type layout structure ({{ screen_type_explicit }}).

================================================================================
CONTEXT
================================================================================
Original requirements JSON (for reference only — do not regenerate from scratch):
{{ requirements_json }}

================================================================================
CURRENT HTML TO REVISE
================================================================================
{{ existing_html }}

================================================================================
GENERATE THE REVISED SCREEN NOW
================================================================================
Start IMMEDIATELY with <!DOCTYPE html>. No text before it.
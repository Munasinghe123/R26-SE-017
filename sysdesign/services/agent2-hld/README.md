# Agent 2: High-Level Architecture Agent (R26-SE-017 Research Component)

Evaluates system requirements, generates candidate architectures via Groq/Ollama, parses them into the Canonical Architecture Model (CAM), classifies architecture style, and evaluates structural quality using 4 universal + 2 style-specific metrics to produce the Composite Architecture Score (CAS).

# HLA Agent v2 — Python Dependencies
# Install: pip install -r requirements.txt

# ── LLM Integration ────────────────────────────────────────────
openai>=1.0.0                  # OpenRouter API (OpenAI-compatible)

# ── Semantic Matching (deterministic) ─────────────────────────
sentence-transformers>=2.2.0   # all-MiniLM-L6-v2 for RTS, CoS (semantic)
torch>=2.0.0                   # Required by sentence-transformers
transformers>=4.30.0           # Underlying transformer models

# ── Graph Analysis ─────────────────────────────────────────────
networkx>=3.0                  # Architecture dependency-graph operations

# ── Scientific Computing ───────────────────────────────────────
scipy>=1.11.0                  # AHP eigenvector, Saaty CR computation
numpy>=1.26.0                  # Array operations
scikit-learn>=1.3.0            # Optional: PCA / clustering for research

# ── Web Server ─────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
pydantic>=2.0.0                # Schema validation (cam/schema.py)
pydantic-settings>=2.0.0       # Settings from .env

# ── Visualization ──────────────────────────────────────────────
matplotlib>=3.9.0              # Radar chart generation

# ── Storage ────────────────────────────────────────────────────
# sqlite3 is bundled with Python — no extra package needed
psycopg2-binary>=2.9.9        # Neon Postgres (shared thesis dataset)

# ── Utilities ──────────────────────────────────────────────────
python-dotenv>=1.0.0           # Load .env file

# ── Testing ────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.24.0
